const Pagination = () => {
    return(
        <nav>
            <ul className="pagination">
                <li>
                    <a href="#" className="page-link">1</a>
                </li>
            </ul>
       </nav>
    )
};

export default Pagination;